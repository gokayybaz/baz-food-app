import React from 'react'

import { IoSearch } from 'react-icons/io5'

const NavbarSearchIcon = ({ iconSize }) => {
    return (
        <IoSearch size={iconSize} />
    )
}

export default NavbarSearchIcon